require 'spec_helper'

describe TaskManager do
  it 'has a version number' do
    expect(TaskManager::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
